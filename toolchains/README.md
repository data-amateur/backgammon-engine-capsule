# Development toolchains

Toolchain lock files belong here; downloaded SDKs do not. Emscripten is kept in
an external cache and its generated output stays under ignored `build/` paths.

See `docs/GNUBG-WASM.md` for the exact setup and verification commands.
